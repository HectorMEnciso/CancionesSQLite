<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/AnadirCancion"
        android:icon="@android:drawable/ic_menu_add"
        android:showAsAction="always"
        android:title="Añadir"></item>
    <item
        android:id="@+id/mainMenu"
        android:icon="@android:drawable/btn_default"
        android:showAsAction="always">

        <menu>
            <item
                android:id="@+id/AnadirCancionOverflow"
                android:icon="@android:drawable/ic_menu_add"
                android:showAsAction="never"
                android:title="Añadir"></item>
            <item
                android:id="@+id/BorrarTodas"
                android:icon="@android:drawable/ic_delete"
                android:title="Borrar todas"></item>
            <item
                android:id="@+id/GuardarFichero"
                android:icon="@android:drawable/ic_menu_save"
                android:showAsAction="never"
                android:title="Guardar en fichero"></item>

        </menu>
    </item>
</menu>